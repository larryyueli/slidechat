{
  "name": "server",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "nodemon app.js",
    "dev": "DEV=1 nodemon app.js",
    "build": "npm run build-c && npm run build-i",
    "build-c":"mkdir client-build; cd ../client && npm run build && cp -r build/* ../server/client-build/ && cd ../server",
    "build-i":  "mkdir instructor-client-build; cd ../instructor-client && npm run build && cp -r build/* ../server/instructor-client-build/ && cd ../server"
  },
  "dependencies": {
    "body-parser": "^1.19.0",
    "compression": "^1.7.4",
    "cookie-parser": "^1.4.4",
    "cors": "^2.8.5",
    "express": "^4.16.1",
    "express-fileupload": "^1.1.7-alpha.3",
    "html-escaper": "^3.0.0",
    "mongodb": "^3.5.7",
    "morgan": "^1.9.1",
    "pdf-image": "^2.0.0"
  },
  "devDependencies": {
    "axios": "^0.19.2",
    "nodemon": "^2.0.4"
  }
}
